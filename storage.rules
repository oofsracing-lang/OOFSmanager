rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow access by default only if authenticated
      allow read, write: if request.auth != null;
    }
  }
}
