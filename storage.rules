rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{seasonId}/uploads/{allPaths=**} {
      allow write: if true;
      allow read: if request.auth != null;
    }

    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
